VITE_API_BASE_URL=http://0.0.0.0:8000/api/v1
# Google Maps JS API key for the Plant Design module's site-capture map
# (address search, roof polygon drawing). Get a real key from the Google
# Cloud console (Maps JavaScript API + Places API enabled) and set it in
# your local, untracked .env.development.local instead of editing this
# tracked file.
VITE_GOOGLE_MAPS_API_KEY=
